import React from 'react';
import Form from "./Form/Form";

const CurrentExchange: React.FC = () => {

    return (
        <div>
            <Form/>
        </div>
    );
};

export default CurrentExchange;